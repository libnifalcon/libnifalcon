INCLUDE_DIRECTORIES(../../src ${INC_DIRS})
LINK_DIRECTORIES(../../lib ${LINK_DIRS})
ADD_EXECUTABLE(findfalcons findfalcons.c)
TARGET_LINK_LIBRARIES(findfalcons ${LIBNIFNAME} ${LIBFTDINAME})
IF(NOT WIN32)
  INSTALL(TARGETS findfalcons RUNTIME DESTINATION "/usr/local/bin")
ENDIF(NOT WIN32)
