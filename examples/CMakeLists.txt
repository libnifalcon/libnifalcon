SET(CPP_DEFINES)
SET(CPP_LINK_LIBS ${LIBNIFALCON_CPP})

IF(LIBUSB_FOUND)
  IF(LIBFTDI_FOUND)
	SET(CPP_DEFINES ${CPP_DEFINES} -DLIBFTDI)
	SET(CPP_LINK_LIBS ${CPP_LINK_LIBS} ${LIBNIFALCON_CPP_COMM_LIBFTDI} ${LIBFTDI_LIBRARIES}) 
  ENDIF(LIBFTDI_FOUND)
ENDIF(LIBUSB_FOUND)

IF(LIBFTD2XX_FOUND)
  SET(CPP_DEFINES ${CPP_DEFINES} -DLIBFTD2XX)
  SET(CPP_LINK_LIBS ${CPP_LINK_LIBS} ${LIBNIFALCON_CPP_COMM_FTD2XX} ${LIBFTD2XX_LIBRARIES})
ENDIF(LIBFTD2XX_FOUND)

ADD_SUBDIRECTORY(findfalcons)
ADD_SUBDIRECTORY(findfalcons_cpp)
IF(GLUT_FOUND)
  ADD_SUBDIRECTORY(falcon_dk_math)
ELSE(GLUT_FOUND)
  MESSAGE("Cannot compile FalconDKMath example - Missing packages")
ENDIF(GLUT_FOUND)

IF(Boost_PROGRAM_OPTIONS_FOUND)
  ADD_SUBDIRECTORY(falcon_test_cli)
ELSE(Boost_PROGRAM_OPTIONS_FOUND)
  MESSAGE("Cannot compile falcon_test_cli - Missing Boost Program Options")
ENDIF(Boost_PROGRAM_OPTIONS_FOUND)

IF(GMTL_FOUND AND OSG_FOUND AND ODE_FOUND AND Boost_FOUND AND GLUI_FOUND)
  ADD_SUBDIRECTORY(FalconMechanics)
ELSE(GMTL_FOUND AND OSG_FOUND AND ODE_FOUND AND Boost_FOUND AND GLUI_FOUND) 
  MESSAGE("Cannot compile FalconMechanics example - Missing packages")
  IF(NOT GMTL_FOUND)
	MESSAGE("- GMTL")
  ENDIF(NOT GMTL_FOUND)
  IF(NOT OSG_FOUND)
	MESSAGE("- OSG")
  ENDIF(NOT OSG_FOUND)
  IF(NOT ODE_FOUND)
	MESSAGE("- ODE")
  ENDIF(NOT ODE_FOUND)
  IF(NOT Boost_FOUND)
	MESSAGE("- Boost")
  ENDIF(NOT Boost_FOUND)
  IF(NOT GLUI_FOUND)
	MESSAGE("- GLUI")
  ENDIF(NOT GLUI_FOUND)
ENDIF(GMTL_FOUND AND OSG_FOUND AND ODE_FOUND AND Boost_FOUND AND GLUI_FOUND)


