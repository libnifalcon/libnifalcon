SET(CPP_DEFINES)
SET(CPP_LINK_LIBS ${LIBNIFALCON_CPP} ${LIBNIFALCON_CPP_FIRMWARE_NOVINTSDK})

IF(LIBUSB_FOUND)
  IF(LIBFTDI_FOUND)
	SET(CPP_DEFINES ${CPP_DEFINES} -DLIBFTDI)
	SET(CPP_LINK_LIBS ${CPP_LINK_LIBS} ${LIBNIFALCON_CPP_COMM_LIBFTDI} ${LIBFTDI} ${LIBUSB}) 
  ENDIF(LIBFTDI_FOUND)
ENDIF(LIBUSB_FOUND)

IF(LIBFTD2XX_FOUND)
  SET(CPP_DEFINES ${CPP_DEFINES} -DLIBFTD2XX)
  SET(CPP_LINK_LIBS ${CPP_LINK_LIBS} ${LIBNIFALCON_CPP_COMM_FTD2XX} ${LIBFTD2XX})
ENDIF(LIBFTD2XX_FOUND)

ADD_SUBDIRECTORY(findfalcons)
ADD_SUBDIRECTORY(findfalcons_cpp)
IF(GLUT_FOUND)
  ADD_SUBDIRECTORY(falcon_dk_math)
ENDIF(GLUT_FOUND)
ADD_SUBDIRECTORY(FalconMechanics)
