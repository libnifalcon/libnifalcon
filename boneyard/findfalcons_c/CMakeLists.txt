# INCLUDE_DIRECTORIES(../../src/c ${INC_DIRS})
# LINK_DIRECTORIES(../../lib ${LINK_DIRS})
INCLUDE_DIRECTORIES(../c_driver)
SET(SRCS findfalcons.c)

IF(LIBFTDI_FOUND)
  SET(LINK_LIBS nifalcon_c_libftdi ${LIBFTDI_LIBRARIES} ${COMM_LIBS} ${LIBMATH})
  SET(DEFINES "-DLIBFTDI")
  BUILDSYS_BUILD_EXE(
	NAME findfalcons_c_libftdi 
	SOURCES "${SRCS}" 
	LINK_LIBS "${LINK_LIBS}" 
	CXX_FLAGS "-DLIBFTDI" 
	LINK_FLAGS FALSE)
ENDIF(LIBFTDI_FOUND)

IF(LIBFTD2XX_FOUND)
  SET(LINK_LIBS ${LIBNIFALCON_C_PREFIX}_ftd2xx ${LIBFTD2XX_LIBRARIES} ${COMM_LIBS} ${LIBMATH})
  SET(DEFINES "-DFTD2XX")
  BUILD_NIFALCON_EXE(findfalcons_c_ftd2xx "${SRCS}" "${LINK_LIBS}" FALSE "-DFTD2XX" FALSE)
ENDIF(LIBFTD2XX_FOUND)

